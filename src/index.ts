/**
 * Export the main ChangesTracker class and the TChange type.
 */
export { ChangesTracker, type TChange } from './changes-tracker';

// Если у вас есть другие экспорты, добавьте их сюда.
// Например, если бы IChangesTrackerOptions не был приватным:
// export type { IChangesTrackerOptions } from './changes-tracker'; 